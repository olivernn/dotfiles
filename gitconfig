[user]
  name = Oliver Nightingale
  email = oliven@amazon.com
[color]
  ui = auto
[core]
  pager = less -FMRiX
  excludesfile = /Users/oliven/.gitignore
  editor = vim
[alias]
  dag = log --graph --format='format:%C(yellow)%h%C(reset) %C(blue)\"%an\" <%ae>%C(reset) %C(magenta)%ar%C(reset)%C(auto)%d%C(reset)%n%s'  --date-order
  assume = update-index --assume-unchanged
  unassume = update-index --no-assume-unchanged
  assumed = "!git ls-files -v | grep ^h | cut -c 3-"
  default-branch = "!git symbolic-ref refs/remotes/origin/HEAD | sed 's@^refs/remotes/origin/@@'"
  select = "!git status -su | fzf -m --preview='git diff --color=always {+2}'"
  select-then = "!git select | xargs -o git"
  fixup = "!git log HEAD...$(git default-branch) --oneline --decorate | fzf --preview='git show --color=always {+1}' | cut -f 1 -d ' ' | xargs -o git commit --fixup"
[grep]
  extendedRegexp = true
  lineNumber = true
  patternType = extended
[init]
  templatedir = ~/.git_templates
[ghostwriter "github.com"]
  email = oliver.nightingale1@gmail.com
[ghostwriter "bitbucket.com"]
  email = oliver.nightingale1@gmail.com
[ghostwriter "git.amazon.com"]
  email = oliven@amazon.com
[push]
	default = simple
[amazon]
	append-cr-url = true
	pull-request-by-default = true
